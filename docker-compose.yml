version: '3.6'
services:
  uwsgi:
    build:
      context: .
      dockerfile: ./uwsgi.Dockerfile
    ports:
      - "5000"
  nginx:
    tty: true
    image: nginx:latest
    build:
      context: .
      dockerfile: ./nginx.Dockerfile
    ports: 
      - 80:80
      - 443:443
